[gd_resource type="Resource" load_steps=2 format=3]

[ext_resource type="Script" path="res://data/rift_pull_def.gd" id="1"]

; THE PULL + THE LINE (sl-0115): the rift arena's physics, every value
; [T]. Prototype ports converted to 60 t/s (HANDOFF §7): oscillation
; rate 0.008->0.004, i-frames 15->30; per-second values ride verbatim.
; Defaults in the script ARE these values — the .tres exists so the
; designer's refinement rounds edit data, never code.

[resource]
script = ExtResource("1")
pull_tiles_per_sec = 1.35
osc_amplitude_rad = 0.45
osc_rate_per_tick = 0.004
player_mult = 1.0
boss_mult = 0.3
bullet_mult = 0.15
passive_drain_per_sec = 0.4
deep_drain_per_sec = 2.2
deep_edge_tiles = 0.8
lives = 3
hit_iframe_ticks = 30
snap_grace_ticks = 90
