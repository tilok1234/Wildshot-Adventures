[gd_resource type="Resource" load_steps=2 format=3]

[ext_resource type="Script" path="res://data/scenario_def.gd" id="1"]

; STARHOOK rift fight, COMMON rarity (S1 seam 6, sl-0105): the
; far-side arena. Entered by casting at a world rift node (the
; driver routes here on CAST_COMPLETE); the exit portal is cell 1,6
; (walk-on, the door language) — it returns you to where you cast.
; Death here = THE LINE SNAPS [T]: no cost, no hardcore stake — you
; just lose the catch (main intercepts before the death flow).

[resource]
script = ExtResource("1")
id = &"rift_common"
display_name = "STARHOOK: rift fight (common)"
arena = "res://data/arena_rift.json"
starhook_rift = true
player_spawn = Vector2(3.5, 6.5)
standin_positions = PackedVector2Array()
enemy_spawns = {
"rift_catch": PackedVector2Array(11.5, 6.5)
}
default_seed = 11

