# Tech Debt Ledger (TECH-20)

Opened at M0 per planning `docs/12-PHASE_A_LAB_BUILD_PLAN.md` §4. Every
deliberate shortcut gets a named entry here in the same commit that introduces
or confirms it. The formal TECH-20 read-out happens after the combat
prototype. The scope tripwire (CLAUDE.md) also ledgers refused out-of-scope
work here.

| # | Entry | Where (lands at) | Exit condition |
|---|---|---|---|
| 1 | Hard-coded ability effects — the three test abilities are bespoke code, no item/skill composition system (TECH-06 deferred) | `sim/` (M4) | Phase C item-system design |
| 2 | Quickdraw is a hard-coded cadence multiplier — no stat pipeline exists (TECH-08 deferred) | `sim/` (M4) | TECH-08 stat-pipeline decision |
| 3 | Fixed class-shell stats — flat values in data, no growth curves | `data/` (M2) | Phase C build grammar |
| 4 | Arena-only spawning — scenario spawner assumes the one greybox arena | `sim/` (M2+) | Phase B+ encounter authoring |
| 5 | Placeholder EffectLibrary entries until the effects pack lands (M-FX; pack-or-placeholder call pre-registered for end of M5) | `game/` (M3+) | Effects-pack acceptance — 9-row checklist (M6) |
| 6 | Brute-force actor collision and projectile-vs-actor circle tests — spatial hash deferred, upgrade path documented | `sim/collision/` (M2) | **CLOSED at M2**: stress verdict PASS (reports/stress_m2.json, min 1950 fps at 600 live) — brute force stays for Phase A; reopen only if a later milestone's density profile breaks budget |
| 7 | PNG-sequence GIF path (armed capture → PNG → ffmpeg) — no in-engine encoder. Amended M3: viewport readback measured **32 ms/capture** on the dev machine, so capture is ARMED-mode only (G toggles; fps dips while armed, REC badge explains). **AMENDED 2026-08-01 (098a679, the designer's want ×3):** the last-10-s RING is RETIRED — G→G now captures START-TO-FINISH, frames streaming to disk as taken (unbounded length, no flush race; the armed 32 ms stall still slows the game and the footage inherits it — the designer feels it as "recordings play slow"). Improvement path unchanged: RenderingDevice async readback (4.4+ API) would remove the stall (and could bring back an always-on retroactive ring as a bonus, not a replacement). | `game/drivers/gif_recorder.gd`, `tools/gif.ps1` (M3, reworked S0 night) | Async readback, or only if the armed slowdown starts hurting the weekly cadence |
| 8 | Kinematics axis-clamp assumes legal (non-overlapping) spawns: a body spawned inside solid geometry gets clamped to the near face on its first move on that axis (can read as a small teleport) and is not evicted on the other axis. Found by the motion probe's own illegal spawn at (10,10). **Partially mitigated M5:** scenario_loader rejects enemy spawns on solid TILES (push_error + skip); circle-overlap-vs-corner legality still unchecked. | `sim/systems/player_move.gd` (M2) | Scenario loader validates full circle spawn legality against the bitgrid |
| 9 | Entry 5 amended: the M-FX call is DECIDED (2026-07-27, curated projectile pack shipped + integrated). **EffectLibrary pass DONE 2026-07-28:** density/opacity scaler + flash-reduction built (`game/views/effect_library.gd`, cosmetic/friendly channels only, hostile clamped structurally — no hostile view holds the reference), Nova cast ring now the pack sprite (`nova_ring` map field), options rows + [effects] persistence live. Still procedural (deliberate, cosmetic): generic muzzle/impact/kill pops. **CLOSED 2026-07-29:** 9-row eye rows 1/2/3/6/9 ACCEPTED via the Decision Deck register (sign-off table filled in notes/NINE_ROW_ACCEPTANCE.md, two-tier basis). | `game/views/flash_view.gd`, options (M6) | Closed by the signed 9-row record |
| 10 | Rusher slash (pattern 11) shares the fang sprite with Fanmaw's fan (pattern 13) — Law 3 wants per-family shapes; the pack's role table predates the slash decision. **CLOSED 2026-07-28:** sphere-pack adoption (designer-directed; planning decision + §2.6 amendment) gives every family its own hue identity — slash owns cyan d10; the share no longer exists. | `data/projectile_map.tres` (M5+) | Closed by the sphere-pack hue map |
| 11 | DodgeBot primary policy models melee chasers as do-not-enter trigger bubbles (conservative); reactive windup-dodging (enter range, dodge on telegraph like a human) is not modeled — first_contact composition FAILS at 3.0 under this model and awaits designer adjudication. **CLOSED 2026-07-29:** designer ruled REACTIVE the policy of record (Decision Deck register, planning tools/decision_deck_register.json). The first_contact adjudication dissolves; the three wolf-pair FAILs become documented primary-model baselines. Canonical battery re-baseline under reactive queued (needs exclusive Godot access). | `game/bots/dodge_policy.gd` (M5) | Closed by the reactive-as-record ruling |
| 12 | Game-side blob-47 mask placement exists for hand-authored arena coverages (Forest Walk grass) — decision 2026-07-27 says resolution logic lives in WorldForge; suggested reading (flagged, designer note pending): temporary, retires if a WorldForge-exported arena replaces the hand-authored testbed. **CLOSED 2026-07-29:** grandfather note recorded via Decision Deck ruling — the game-side placement stays, sanctioned, until a WorldForge-exported arena replaces the hand-authored forest. | `game/arena/arena_builder.gd` (M5+) | Closed by the recorded grandfather note |
| 13 | DodgeBot report near-miss metric samples projectiles and contact bodies only — hazard proximity is unsampled, so a hazard-only enemy (Blightcaster) reads near_miss −1 ("nothing near") even while zones are dodged. **CLOSED 2026-07-28 (M7):** armed hostile zones now sampled in `_nearest_threat_clearance` (pre-arm zones stay unsampled — warnings, not threats). Semantics note: NEGATIVE hazard clearance = the bot inside an armed zone between pulses, which is legal pulse-train play; verdict remains the evidence. Canonical reports rerecord at the pack-intake battery (runbook). | `game/bots/dodge_proof.gd` (M6) | Closed — metric live; reports rerecord at intake |
| 14 | Lingering armed zones render as the full arm ring (progress clamped 1.0) — no distinct "burning" visual state; fill/rim split per §2.5 is done but the burn state is v0. **Treatment BUILT 2026-07-28 (EffectLibrary pass):** the arm strip refills toward each pulse from the sim's own serialized cadence (next_damage_tick/hit_interval) over a solid armed rim, plus an additive hot fill swell — emphasis only, nothing renders below pre-arm visibility. Also fixed in passing: zone sprites are now keyed by the hazard's own pattern id (faction-keyed lookup was the documented pre-SERIAL-11 stopgap). **CLOSED 2026-07-29:** rode the 9-row acceptance (row 1 covers zone rims/arm strips at stress). | `game/views/hazard_view.gd` (M6) | Closed with the 9-row sign-off |
| 17 | Fit-rule round-1 scope (sl-0078, deliberate): ART-MATCHED prop collision applies to WORLDFORGE pack worlds only — arena-def props (lab furniture, Forest Walk blob-47 masks) still block full-cell, and enemies stay grid-walkers everywhere (accepted asymmetry). Structures/fences keep full cells (the between-house slits question rides later per the ruling). | `game/arena/prop_colliders.gd` (sl-0078) | Designer feel round after the red-line walk — extend to arena defs / enemies / structures only by their own deliberate calls |
| 16 | Replay headers carry seed + speed but NOT the Loop v1 character block (gold/xp/level/tiers/armor) — a profile-run .wsr rebuilds from scenario defaults, so its initial hash honestly REFUSES verification (visible failure, never silent corruption). Proof/golden runs are unaffected: they are profile-free by construction (audit/verify skip the profile; bot_runner never loads main). **AMENDED slice S0 seam 1 (sl-0100, SERIAL 15):** the stat-frame profile fields (class, ring) and the class LOADOUT swap (the class frame replaces the lab trio at profile apply) join the same recorded gap — profile replays still refuse honestly, nothing new fails silently. **AMENDED S1 (sl-0104/0105/0113, SERIAL 18–21):** the unique armor item, the multi-active quest state (taken/done masks + progress), the gather/rift-node fields, and the profile's starhook lane all join the same gap. NOTE: WSR bumped to VERSION 2 at the interact era (InputFrame grew the interact byte; v1 replays refuse loudly) — a FORMAT bump, not the character block; the header still carries no profile. | `input/replay_recorder.gd`, `input/replay_format.gd` (Loop v1) | Replay format grows a character block (header version bump) when loop replays need to verify — likely alongside the slice daily-play bar |
| 15 | **WorldForge structure collision is POROUS — the dominant "walking bugs out near structures" cause (cell-grid evidence 2026-07-28, region 208-248 x 126-148):** buildings are emitted as solid wall CHUNKS with single-cell walkable slots between them (window columns, segment gaps) plus walkable lanes/pockets behind and inside facades — rows read `SS.SS.SS` across one visually continuous house. The 0.7-diameter player fits the 1-wide slots: brushing a facade slides INTO the building's visual outline (player renders over the sprite, snags in corner-heavy micro-geometry). The game honors "collision = walkability verbatim" correctly — the data is the artifact. Also in this entry: north roof-ridge rows solid-layer overlap (secondary), door-column crossings (intended). Canopy-banding `props-overhang` fixed the awning half only. | WorldForge exporter (read-only here) | **CLOSED 2026-07-29 — the whole class resolved by the WYSIWYG arc:** a1304b9 stamped full footprints (porosity dead), then the designer's cities-open ruling landed the honest endstate upstream (WF WYSIWYG commit: collision = art outline minus declared pass cells, bidirectional gate, seal machinery deleted). Game intake: flood 34739 pack accepted in play ("hallelujah! finnaly works"); porosity diag re-pinned 44 with the per-drop doctrine. |
