# Godot 4
.godot/
*.translation

# Export / build output
builds/
export_presets.cfg.secret

# Bot harness output (mechanical verification only — not committed)
reports/*
!reports/.gitkeep
!reports/.gdignore

# Regenerable test outputs (pixel-match diffs, arena previews)
tests/pixel_match/*.png

# OS noise
Thumbs.db
Desktop.ini
.DS_Store
